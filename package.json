{
  "name": "highlighttext",
  "version": "1.0.0",
  "private": true,
  "description": "Highlight text while reading. Copy what matters. No clutter.",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node -e \"require('fs').copyFileSync('manifest.json', 'dist/manifest.json')\" && node -e \"require('fs').copyFileSync('highlighttext.png', 'dist/highlighttext.png')\"",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/jsdom": "^21.1.7",
    "jsdom": "^24.0.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-code": "^2.0.50"
  }
}
